latex_dir = "../../Tesi_Andrea_Grande/";
subdirs = [ ...
    "Chapter_1/equivalent_airspeed_basic_2/", ...
    "Chapter_1/equivalent_airspeed_basic_3/" ...
    ];

% create folders (skipped if folders exist)
for i=1:length(subdirs)
    [status, msg] = mkdir(strcat(latex_dir, subdirs(i)));
end

files = [ ...
    "test_0.m", "test_0.pdf", ...
    "test_1.m", "test_1.pdf", ...
    ];

for i=1:length(files)
    status = copyfile(files(i), strcat(latex_dir, subdir, files(i)));
end
